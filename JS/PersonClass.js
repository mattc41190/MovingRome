var person = new function(first, last, times, color, data, polylineOptions, locations) {
    this._firstName = first;
    this._lastName = last;
    this._dates = times;
    this._color = color;
    this._data = data;
    this._polylineOptions = polylineOptions;
    this._locations = locations;
}